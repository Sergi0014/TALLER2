function showChart() {
    // Muestra el elemento que contiene los porcentajes
    document.getElementById('cityPercentages').style.display = 'block';
}

function hideChart() {
    // Oculta el elemento que contiene los porcentajes
    document.getElementById('cityPercentages').style.display = 'none'  
}